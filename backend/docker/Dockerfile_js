FROM node:20-slim

# Устанавливаем рабочую директорию
WORKDIR /usr/src/app

# Копируем файлы в контейнер
COPY . /usr/src/app

# Указываем команду для запуска приложения (предполагается, что приложение запускается через файл, например app.js)
CMD ["node", "app.js"]